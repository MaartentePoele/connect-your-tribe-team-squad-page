<!doctype html>
<html lang="nl">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="assets/styles/style.css">
    <title>Squad Page</title>
  </head>
  <body>
    <header>
      <h1>Squad Page</h1>
      <div class="search-box">
        <input type="search" name="search" placeholder="Zoek op naam">
        <button type="submit" class="search-btn">
          <img src="/assets/styles/search-icon.png" class="search-icon" alt="zoeken">
        </button>
      </div>
    </header>

    <main>
      <aside>
        <section class="sidebar">
        <h2>Sorteer op:</h2>
        <label>Naam:</label>
        <select>
          <option value="a-z">A-Z</option>
          <option value="z-a">Z-A</option>
        </select>

        <h2>Filter op</h2>
        <details>
        <summary>
        Squads
        </summary>
          <a class="navKnop" href="?filter=1I">1I</option>
          <a class="navKnop" href="?filter=1J">1J</option>
        </details>
     
        <details>
        <summary>
        Teams
        </summary>
          <a class="navKnop" href="/">All</a> 
          <a class="navKnop" href="?filter=Cheer">Cheer</a>
          <a class="navKnop" href="?filter=Dazzle">Dazzle</a>
          <a class="navKnop" href="?filter=Glow">Glow</a>
          <a class="navKnop" href="?filter=Harmony">Harmony</a>
          <a class="navKnop" href="?filter=Radiant">Radiant</a>
          <a class="navKnop" href="?filter=Rocket">Rocket</a>
          <a class="navKnop" href="?filter=Spark">Spark</a>
          <a class="navKnop" href="?filter=Sunny">Sunny</a>
        </details>


        <label>Emoji:</label>
        <select>
          <option value="🧌">🧌</option>
          <option value="🍗">🍗</option>
          <option value="❤️">❤️</option>
          <option value="🫡">🫡</option>
          <option value="🤑">🤑</option>
          <option value="👀">👀</option>
          <option value="🦧">🦧</option>
          <option value="🪤">🪤</option>
          <option value="🫧">🫧</option>
          <option value="💅🏻">💅🏻</option>
        </select>

        <label>Favorieten:</label>
        <input type="checkbox">

        <button>Submit</button>
      </section>
      </aside>

      <section>
        {% for person in persons %}
          <article class="container">
             <svg xmlns="http://www.w3.org/2000/svg" width="40px" height="40px" viewBox="0 0 24 24" fill="white" stroke="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-heart-icon lucide-heart"><path d="M2 9.5a5.5 5.5 0 0 1 9.591-3.676.56.56 0 0 0 .818 0A5.49 5.49 0 0 1 22 9.5c0 2.29-1.5 4-3 5.5l-5.492 5.313a2 2 0 0 1-3 .019L5 15c-1.5-1.5-3-3.2-3-5.5"/></svg>
            <figure>
              {% if person.mugshot %}
                <img src="https://fdnd.directus.app/assets/{{ person.mugshot }}?width=500" alt="Profile picture">

              {% else %}
                <img
                  src="https://www.shutterstock.com/image-vector/blank-avatar-photo-placeholder-flat-600nw-1151124605.jpg"
                  alt=""
                >
              {% endif %}
              <figcaption>
                <h2>{{person.name}}</h2>
                {{person.vibe_emoji}}
               

              </figcaption>
            </figure>
            <article class="overlay overlaybottom">
              <h3>{{person.name}} </h3>
              <h3>Bio:</h3>
              <p>  {{person.bio }}</p>
              <h3>Squad:</h3>
              <p>1J</p>
              <h3>Team:</h3>
              <p>{{person.team }}</p>
            </article>
          </article>
        {% endfor %}
      </section>
    </main>

    {% comment %}
      {% for message in messages %}
        <p>{{ message.from }}: {{ message.text }} @{{ message.created | date: '%d-%m-%Y %H:%M' }}</p>
      {% else %}
        <p>Er zijn nog geen berichten voor dit team.</p>
      {% endfor %}

      <h2>Voeg een bericht toe</h2>
      <form method="post" action="/">
        <label>
          Naam:
          <input type="text" name="from" required>
        </label>
        <label>
          Bericht:
          <textarea name="text" required></textarea>
        </label>
        <button type="submit">Verstuur</button>
      </form>
    {% endcomment %}
  </body>
</html>
